# Profiles

One directory per product or hardware target. Copy `example/` to create a
profile, add configuration fragments or feed declarations beside `profile.env`,
and keep source-level deltas in `../patches/<profile>/`.
