# Example only. Copy this directory to profiles/<product> and set values for
# your firmware upstream.  Values are evaluated by packaging/build.sh.
UPSTREAM_BUILD_COMMAND='make defconfig world'
ARTIFACT_GLOB='bin/targets/**/*.bin'
